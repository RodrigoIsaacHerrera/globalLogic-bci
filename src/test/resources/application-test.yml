spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb

server:
  servlet:
    session:
      cookie:
        http-only: false

logging:
  level:
    org.springframework.security: DEBUG